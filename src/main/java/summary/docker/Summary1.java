package summary.docker;

public class Summary1 {
    /**
     * docker扫盲
     * 解决测试的环境问题导致的bug
     * 容器技术
     * docker仓库、镜像
     * docker把一些代码和环境集中在一起 个人只需要拉取就直接可以启动使用了
     * 去仓库拉别人打包好的镜像文件复现CVE
     * 查看镜像：docker ps、docker images（版本、大小、ID、创建时间）
     * 搜索镜像:docker search
     * 拉取镜像:docker pull
     * 删除镜像：docker rmi id
     * docker run
     *
     * 数据卷：宿主机的一个目录，映射到容器里，可以在宿主机里操作这个目录，那么容器里映射的文件也会一起改变
     * （有点类似挂载）
     *
     *
     */
}
